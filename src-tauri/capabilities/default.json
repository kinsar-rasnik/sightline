{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability granted to the main window. Phase 1 scope: health command only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

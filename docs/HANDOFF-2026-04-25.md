# HANDOFF — Phase 6 (Housekeeping + Multi-View Sync Engine)

**Datum:** 2026-04-25
**Verantwortlich:** CTO (Übergabe-Dokument)
**Phase / Block:** Phase 6 — Housekeeping (PR #15) + proper / Multi-View Sync Engine (PR #16)

---

## Was wurde geliefert

**Phase 6 Housekeeping (PR #15, Squash-Commit `24211d3`):**
- Migration `0009_completion_threshold.sql`: completionThreshold via `app_settings` (Schema 8 → 9)
- Player-Keyboard-Handler auf Shortcuts-Service migriert (15 neue `player_*` ActionIds)
- Per-VOD-Volume-Memory in localStorage persistent
- CI-Bump `actions/cache@v4` → `@v5` (beide Call-Sites)
- Player-Coverage-Erweiterung (Vitest, Playwright deferred)
- Workforce-Infrastruktur: STATE.md, HANDOFF-Template, .claude/CHANGELOG.md

**Phase 6 proper — Multi-View Sync Engine (PR #16, Squash-Commit `d08b16b`):**
- Split-View v1 mit 2 Panes 50/50, Wall-Clock-Sync (Unix-Sekunden), 250ms Drift-Toleranz
- Backend Sync-Service mit Session-Orchestrierung
- 11 neue Tauri-Commands (8 spec'd + cmd_get_overlap + 2 für Event-Delivery)
- 5 neue Events: sync:state_changed, sync:drift_corrected, sync:leader_changed, sync:member_out_of_range, sync:group_closed
- Frontend `/multiview`-Route, Zustand-Store, 250ms-Tick-Sync-Loop
- Group-wide Transport (Play/Pause/Seek/Speed)
- Per-Pane Audio-Mix (Volume + Mute)
- Out-of-Range-Handling mit Auto-Pause-Overlay
- Co-Streams-Panel-Entry mit Multi-Select
- JS↔Rust Parity-Fixture (`sync-math.fixture.json`) — gemeinsame Test-Vectors für beide Math-Implementierungen, Divergenz bricht CI auf beiden Seiten
- Integration-Smoke-Test (`sync_smoke.rs`) und Math-Parity-Test (`sync_math_parity.rs`)
- 34 Vitest-Cases im Frontend
- Migrationen `0010_sync_sessions.sql` + `0011_sync_settings.sql` (Schema 9 → 11)

**Zusammen:** 25 Commits squashed-on-main (7 Housekeeping + 18 proper), +6.852 / -214 Zeilen, 39+ Files.

---

## Was wurde NICHT geliefert / verschoben

**Aus Phase 6 Housekeeping verschoben:**
- Playwright + Tauri-driver E2E-Coverage für Player → Phase 7 oder eigener Hotfix-Run. Vitest-Coverage substituiert für jetzt. Braucht Fixture-MP4-Generation-Script + Mock-IPC-Layer.

**Aus Phase 6 proper als Follow-ups dokumentiert (10 Items in session-report):**
- use-sync-loop post-seek Telemetry
- useSyncLoop dependency-array Doc-Clarity
- SyncService cache `sync_drift_threshold_ms` (Per-Tick-DB-Read vermeiden)
- Per-Pane Volume/Mute-Persistenz in `sync_session_panes` (für v2 Resume)
- Asset-Protocol Scope-Narrowing (ADR-0019 → Phase 7)
- v2-Themen: PiP-Layout, >2 Panes, Crossfader, Latency-Aware Sync, Shareable Sync-URLs

---

## Neue ADRs

- **ADR-0021** — Split-View v1 Layout & UI Topologie
- **ADR-0022** — Sync-Math & Drift-Korrektur (250ms-Schwelle, Korrektur-Strategie)
- **ADR-0023** — Group-wide Transport & Leader-Election

---

## Neue Migrationen

- **0009_completion_threshold.sql** — Schema 8 → 9 (Phase 6 Housekeeping)
- **0010_sync_sessions.sql** — Schema 9 → 10 (sync_sessions + sync_session_panes Tables)
- **0011_sync_settings.sql** — Schema 10 → 11 (sync_drift_threshold_ms, sync_default_layout, sync_default_leader in app_settings)

`LATEST_SCHEMA_VERSION = 11`. In-Memory- und On-Disk-Migration-Tests grün.

---

## CI / Quality Gates

**PR #15 (Housekeeping):**
- CI-Run-URL: https://github.com/kinsar-rasnik/sightline/actions/runs/24937542360
- Alle 5 Jobs grün (checks, test ubuntu/macos/windows, audit)

**PR #16 (Multi-View Sync):**
- CI-Run-URL: https://github.com/kinsar-rasnik/sightline/actions/runs/24939338377
- Alle 5 Jobs grün (checks 2m05s, test ubuntu 2m06s, test macos 1m37s, test windows 6m01s, audit 3m09s)

Lokale Gates (vor jedem Commit grün):
- pnpm typecheck ✅
- pnpm lint ✅
- pnpm test ✅
- cargo fmt --check ✅
- cargo clippy --all-targets --all-features -- -D warnings ✅
- cargo test --all-features ✅

---

## Sicherheitsbefunde

**PR #15:** Keine Findings.

**PR #16:**
- Code-Reviewer: 2 P0 Findings (record_drift / report_out_of_range Validation-Gap; apply_transport misleading StateChanged{Active}-Emission) — beide inline gefixt in `2529403`. P1/P2 als Follow-ups dokumentiert.
- Security-Reviewer: 0 Critical, 0 High, 2 Medium (matched die P0s + unbounded `overlap_of`-Length), 3 Low. Alle Medium inline gefixt. 2 Low gefixt (debug log discriminant, pane_index membership check). 1 Low confirmed expected-state (Capability-Narrowing — als Phase-7-Follow-up via ADR-0019).
- `cargo audit`: RUSTSEC-2023-0071 (medium, `rsa 0.9.10` via sqlx-mysql) trägt sich weiter — sqlx-mysql-Feature ist in Sightline nicht aktiviert, Dependency ist im Lockfile aber nicht im Build-Graph. Per ADR-0017 informational bis Phase 6, in Phase 7 zu härten.

---

## Open Questions / Follow-Ups

**Für Phase 7 vorgemerkt:**
1. Asset-Protocol Scope-Narrowing (ADR-0019, war im security-Low-Befund)
2. SyncService Settings-Caching (per-Tick-DB-Read eliminieren)
3. cargo-audit-Härtung (ADR-0017 sieht Phase 7 als Härtungs-Punkt vor)

**Als Pickup-Items im Phase-7-Prompt empfohlen, falls Scope erlaubt:**
- Playwright + Tauri-driver E2E (kann auch eigener Post-1.0-Run werden)
- Per-Pane Volume/Mute-Persistenz (eher v1.1, nicht v1.0-blocking)

**Post-1.0 (kein Phase-7-Scope):**
- Auto-Issue-Triage-Workflow via Claude Desktop + GitHub (eigenes Folge-Projekt)
- v2-Themen: PiP, >2 Panes, Crossfader, Latency-Aware Sync, Sync-URL-Sharing

---

## Synthetic Workforce Änderungen

**Konventionsänderung (Eintrag in `.claude/CHANGELOG.md`):**
- **Senior Engineer macht Merge + Tag, nicht CEO.** Vom CEO am 2026-04-25 explizit so festgelegt. Hintergrund: CEO ist nicht-technisch, Web-UI-Klick-Workflow ist umständlich, Senior Engineer hat ohnehin gh-CLI und kann atomar Merge + Tag durchführen. CEO-Freigabe erfolgt über PR-Approval und explizite Anweisung "merge das jetzt".

**Workforce-Infrastruktur etabliert (Phase 6 Housekeeping):**
- `docs/STATE.md` als Pflicht-Briefing
- `docs/HANDOFF-TEMPLATE.md` als Übergabe-Vorlage
- `.claude/CHANGELOG.md` als Tracker für Workforce-Drift

Erste produktive Anwendung: dieser HANDOFF.

---

## Empfehlung für nächsten Step

**Phase 7 als Single-Run-Mission, gleiche Long-Run-Konvention wie Phase 6 proper.**

Branch: `phase-7/release-pipeline` (von `main` @ `d08b16b`)

Mission-Skelett:
1. Auto-Cleanup-Service (ADR + Migration 0012 + Service + Commands + Settings + UI)
2. GitHub Release Workflow (`.github/workflows/release.yml`, Tag-Push triggert Build-Matrix → Tauri-Bundle pro OS → Asset-Upload)
3. Update-Checker (Backend gegen GitHub Releases API + UI-Notification + AppSettings-Opt-in)
4. Release-Notes-Generator aus Conventional Commits
5. Install-Doku (Unsigned-Binary-Hinweise pro OS)
6. README v1.0-Polish
7. Phase-6-Pickup-Items: Asset-Protocol-Narrowing, SyncService-Settings-Caching, cargo-audit-Härtung
8. ADRs, Tests, Subagent-Reviews, Session-Report
9. Final: CC mergt, taggt `v1.0.0`, GitHub-Release wird durch Tag-Push automatisch publiziert

**Locked Decisions für Phase 7:**
- OSS-Distribution, unsigned binaries, kein Signing/Notarization
- Tag-Push als Release-Trigger
- Update-Checker opt-in via Settings (Default: aus, Privacy-aware)
- Senior Engineer mergt + taggt selbst (kein CEO-Block)

Voraussetzung erfüllt: Repo clean, main aktuell, alle Tags gesetzt. Phase 7 kann sofort starten.
